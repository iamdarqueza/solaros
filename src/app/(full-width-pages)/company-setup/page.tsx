import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Company Setup | SolarOS",
  description: "Create or join a SolarOS organization workspace.",
};

export default function CompanySetupPage() {
  return null;
}
